{
  "name": "chess-notation-lint",
  "version": "0.1.0",
  "description": "Lint PGN chess movetext for notation mistakes",
  "private": true,
  "scripts": {
    "build": "tsc",
    "test": "tsc && node --test dist/test",
    "lint": "tsc && node dist/src/cli.js"
  },
  "license": "MIT"
}
